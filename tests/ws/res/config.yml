admin_user:
  name: chef
  id: eocdb_administrator
  password: eocdb_chef
  roles: [admin, submit]

databases:
  default:
    type: eocdb.db.mongo_db_driver.MongoDbDriver
    parameters:
      mock: true

links: 'tests/ws/controllers/res/links.md'
